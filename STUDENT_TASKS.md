# Tasks

Runs on a laptop from `events/`. Read `PAPER_OUTLINE.md` first.

1. Set up: `pip install numpy scipy scikit-learn matplotlib`.
2. Run `analysis/analyze_forward.py`; check it matches `RESULTS.md`.
3. Say why co-located caches are the key test.
4. Open one `events/*.mat`; find the cache/retrieval arrays.
5. Correlate one retrieval to its own cache vs other same-site caches.
6. Reproduce a figure.
7. Build a missing figure.
8. Redo the test per bird; explain IND106.
9. Try a decoding read instead of cosine.
10. Test whether the effect depends on caches-per-site.
